module m1
  interface
  [ opaque t
    z:t
    s : (t -> t)
    is_z? : (t -> bool) ]
  body
  [ type t = int
    z = 33
    s = proc (x : t) { x+1 }
    is_z? = proc (x : t) { zero?(x-z) } ]
proc (x : from m1 take t) {
 ((from m1 take is_z?) (x-0))}
